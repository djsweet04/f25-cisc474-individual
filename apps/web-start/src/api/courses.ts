//import { backendFetcher } from '../integrations/fetcher'

// Define reusable, typed fetchers
// export const fetchCourses = backendFetcher<any[]>('/courses')
// export const fetchCourse = (course_cuid: string) =>
//   backendFetcher<any>(`/courses/${course_cuid}`)()
// export const fetchAssignments = (course_cuid: string) =>
//   backendFetcher<any[]>(`/courses/${course_cuid}/assignments`)()
